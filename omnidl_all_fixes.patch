diff -ruN '--exclude=*.pyc' '--exclude=__pycache__' omnidl_audit3/OmniDL-main/domain/models/download_task.py omnidl_fixed/domain/models/download_task.py
--- omnidl_audit3/OmniDL-main/domain/models/download_task.py	2026-03-09 11:22:29.000000000 +0000
+++ omnidl_fixed/domain/models/download_task.py	2026-03-10 05:04:51.091202974 +0000
@@ -90,10 +90,8 @@
         default_factory=threading.RLock, compare=False, repr=False
     )
 
-    def __post_init__(self) -> None:
-        # _pause_event is pre-set by _set_event(); the download thread can
-        # proceed immediately and only blocks when pause() clears the event.
-        pass
+    # CQ-04 FIX: __post_init__ removed — it only contained `pass`.
+    # _pause_event is pre-set by default_factory=_set_event (see field def above).
 
     # ── Convenience properties ────────────────────────────────────────────
 
@@ -135,8 +133,11 @@
                 self.status = DownloadStatus.DOWNLOADING
 
     def cancel(self) -> None:
-        self._cancel_event.set()
-        self._pause_event.set()   # unblock any waiting hook
+        # CONC-02 FIX: acquire _lock so cancel() participates in the same
+        # mutual-exclusion domain as pause()/resume() and snapshot().
+        with self._lock:
+            self._cancel_event.set()
+            self._pause_event.set()   # unblock any waiting hook
 
     @property
     def is_cancellation_requested(self) -> bool:
diff -ruN '--exclude=*.pyc' '--exclude=__pycache__' omnidl_audit3/OmniDL-main/infrastructure/config/config_manager.py omnidl_fixed/infrastructure/config/config_manager.py
--- omnidl_audit3/OmniDL-main/infrastructure/config/config_manager.py	2026-03-09 11:22:29.000000000 +0000
+++ omnidl_fixed/infrastructure/config/config_manager.py	2026-03-10 05:04:51.094203255 +0000
@@ -85,6 +85,13 @@
                 loaded = json.load(f)
             with self._lock:
                 self._data.update(loaded)
+                _snapshot = dict(self._data)
+            # CONFIG-01 FIX: populate the class-level cache after a disk load
+            # so that a second ConfigManager instance for the same path sees
+            # consistent data without reading from disk again.
+            path_key = str(self._path.resolve())
+            with ConfigManager._cache_lock:
+                ConfigManager._cache[path_key] = _snapshot
             logger.debug("Config loaded from %s", self._path)
         except Exception as exc:
             logger.warning("Could not load config (%s) — using defaults", exc)
diff -ruN '--exclude=*.pyc' '--exclude=__pycache__' omnidl_audit3/OmniDL-main/infrastructure/downloader/yt_dlp_engine.py omnidl_fixed/infrastructure/downloader/yt_dlp_engine.py
--- omnidl_audit3/OmniDL-main/infrastructure/downloader/yt_dlp_engine.py	2026-03-09 11:22:29.000000000 +0000
+++ omnidl_fixed/infrastructure/downloader/yt_dlp_engine.py	2026-03-10 05:04:51.060200069 +0000
@@ -44,12 +44,13 @@
     if not cookie_file:
         return None
     cp = Path(cookie_file).resolve()
-    # Accept files inside the OmniDL data directory OR anywhere under the
-    # user's home directory.  Path.parents is used (not str.startswith) to
-    # prevent the sibling-directory bypass (CWE-22).
+    # SEC-01 FIX: only the OmniDL data directory is accepted.
+    # Path.home() was removed — it allowed ~/.ssh/id_rsa and similar
+    # credential files to be forwarded to remote servers as cookiefile.
+    # Use the COOKIE-01 fix in settings_tab.py to auto-copy cookie files
+    # into the data directory so legitimate use cases still work.
     safe_roots = (
         config.config_path.parent.resolve(),
-        Path.home().resolve(),
     )
     is_safe = any(cp == root or root in cp.parents for root in safe_roots)
     if cp.is_file() and is_safe:
@@ -99,11 +100,8 @@
 
 # Known URL patterns that yt-dlp cannot handle, with actionable messages.
 # Checked before calling yt-dlp to give a better UX than a generic error.
-# Patterns that are ALWAYS blocked (no cookie can help)
-_ALWAYS_BLOCKED: list[tuple[re.Pattern, str]] = [
-    # NOTE: Facebook Stories were previously here but have been moved to
-    # _NEEDS_COOKIES — yt-dlp CAN download them when valid cookies are supplied.
-]
+# CQ-01 FIX: _ALWAYS_BLOCKED removed — it was an empty list whose loop
+# body never executed (dead code).  Only _NEEDS_COOKIES remains.
 
 # Patterns that require cookies — only blocked if no cookie is configured
 _NEEDS_COOKIES: list[tuple[re.Pattern, str]] = [
@@ -134,13 +132,11 @@
 
 def _check_unsupported_url(url: str, has_cookies: bool = False) -> str | None:
     """Return a user-friendly message if URL is blocked, else None.
-    
+
     has_cookies=True means a cookie file or browser cookies are configured,
     so cookie-required URLs (Stories, Live) are allowed through to yt-dlp.
+    CQ-01: _ALWAYS_BLOCKED loop removed — list was always empty.
     """
-    for pattern, message in _ALWAYS_BLOCKED:
-        if pattern.search(url):
-            return message
     if not has_cookies:
         for pattern, message in _NEEDS_COOKIES:
             if pattern.search(url):
@@ -451,7 +447,8 @@
 
         _final_filepath: list[str] = []   # mutable closure cell
 
-        _original_pp_hook = opts.get("postprocessor_hooks", [None])[0]
+        _hooks = opts.get("postprocessor_hooks") or []
+        _original_pp_hook = _hooks[0] if _hooks else None  # CQ-03
 
         def _capturing_pp_hook(d: dict) -> None:
             # Capture filepath after EVERY postprocessor finishes — the last
@@ -496,10 +493,14 @@
                 # Remove any partial .part files left by yt-dlp so the download
                 # directory does not accumulate stale fragment files.
                 try:
+                    # ERR-02 FIX: removed the "f.stem in task.filename"
+                    # substring check — it could delete .part files of other
+                    # concurrent downloads whose stem happened to be a substring
+                    # of this task's filename.  glob("*.part") is already
+                    # scoped to output_dir so no broader filter is needed.
                     for f in output_dir.glob("*.part"):
-                        if task.filename and f.stem in task.filename:
-                            f.unlink(missing_ok=True)
-                            logger.debug("Cleaned up partial file: %s", f)
+                        f.unlink(missing_ok=True)
+                        logger.debug("Cleaned up partial file: %s", f)
                 except OSError as cleanup_exc:
                     logger.warning("Part-file cleanup failed: %s", cleanup_exc)
                 raise  # let _run_task handle the CANCELLED transition
@@ -530,7 +531,7 @@
                     if f.suffix.lower() in _MEDIA_EXTS
                     and not f.name.endswith(".part")
                     and not f.name.endswith(".ytdl")
-                    and f.stat().st_size > 50_000      # >50KB — skip thumbnails
+                    and f.stat().st_size > 1_000       # CQ-02: 1KB threshold (thumbnails are .webp/.jpg, excluded by _MEDIA_EXTS)
                 ]
                 if candidates:
                     task.filename = str(max(candidates, key=lambda f: f.stat().st_size))
@@ -555,35 +556,41 @@
 
             status = d.get("status", "")
             if status == "downloading":
-                task.status = DownloadStatus.DOWNLOADING
-                task.downloaded_bytes = d.get("downloaded_bytes") or 0
-                task.total_bytes = (
-                    d.get("total_bytes") or d.get("total_bytes_estimate") or 0
-                )
-                if task.total_bytes > 0:
-                    task.progress = min(
-                        99.0, task.downloaded_bytes / task.total_bytes * 100
-                    )
+                # CONC-01 FIX: wrap all multi-field writes in task._lock so
+                # the UI poll thread's snapshot() call never reads torn state
+                # (e.g. downloaded_bytes advanced past old total_bytes).
+                # callback() is called OUTSIDE the lock to prevent deadlock.
                 speed = d.get("speed")
-                if speed:
-                    task.speed = _fmt_speed(speed)
-                eta = d.get("eta")
-                if eta is not None:
-                    task.eta = _fmt_eta(eta)
+                eta   = d.get("eta")
                 _fname = d.get("filename")
-                if _fname and Path(_fname).is_absolute():
-                    task.filename = _fname
+                with task._lock:
+                    task.status = DownloadStatus.DOWNLOADING
+                    task.downloaded_bytes = d.get("downloaded_bytes") or 0
+                    task.total_bytes = (
+                        d.get("total_bytes") or d.get("total_bytes_estimate") or 0
+                    )
+                    if task.total_bytes > 0:
+                        task.progress = min(
+                            99.0, task.downloaded_bytes / task.total_bytes * 100
+                        )
+                    if speed:
+                        task.speed = _fmt_speed(speed)
+                    if eta is not None:
+                        task.eta = _fmt_eta(eta)
+                    if _fname and Path(_fname).is_absolute():
+                        task.filename = _fname
                 if callback:
                     callback(task)
 
             elif status == "finished":
-                task.status = DownloadStatus.PROCESSING
-                task.progress = 99.5
-                task.speed = ""
-                task.eta = ""
                 _fname = d.get("filename")
-                if _fname and Path(_fname).is_absolute():
-                    task.filename = _fname
+                with task._lock:
+                    task.status = DownloadStatus.PROCESSING
+                    task.progress = 99.5
+                    task.speed = ""
+                    task.eta = ""
+                    if _fname and Path(_fname).is_absolute():
+                        task.filename = _fname
                 if callback:
                     callback(task)
 
@@ -625,13 +632,12 @@
             "max_sleep_interval",
             "geo_bypass",
             "geo_bypass_country",
-            "no_check_certificates",
             "write_all_thumbnails",
             "write_description",
             "write_info_json",
             "age_limit",
             "user_agent",
-        })
+        })  # SEC-02: no_check_certificates removed
 
         raw = self._config.extra_args.strip()
         if not raw:
diff -ruN '--exclude=*.pyc' '--exclude=__pycache__' omnidl_audit3/OmniDL-main/main.py omnidl_fixed/main.py
--- omnidl_audit3/OmniDL-main/main.py	2026-03-09 11:22:29.000000000 +0000
+++ omnidl_fixed/main.py	2026-03-10 05:04:51.091202974 +0000
@@ -146,15 +146,24 @@
     logger = logging.getLogger("omnidl.main")
     stored = config.get("app_version", "")
     if stored != _APP_VERSION:
-        logger.info(
-            "App version changed (%r -> %r) — resetting settings and clearing history",
-            stored, _APP_VERSION,
-        )
-        history.clear()
-        # BUG-2 FIX: reset all settings to factory defaults so settings from
-        # an older build (incompatible feature flags, renamed keys, etc.) do
-        # not carry forward into the new version.
-        config.reset_to_defaults()
+        # UX-01 FIX: only do a full reset when the MAJOR version number changes.
+        # Patch and minor releases (16.0.x → 16.0.y, 16.0.x → 16.1.0) preserve
+        # user settings and download history.  This prevents silent data loss on
+        # every update, while still allowing a clean slate for breaking changes.
+        import re as _re
+        _major = lambda v: (_re.match(r"(\d+)", v) or ["0"])[0]
+        if _major(stored or "0") != _major(_APP_VERSION):
+            logger.info(
+                "Major version changed (%r -> %r) — resetting settings and clearing history",
+                stored, _APP_VERSION,
+            )
+            history.clear()
+            config.reset_to_defaults()
+        else:
+            logger.info(
+                "Version updated (%r -> %r) — preserving user data",
+                stored, _APP_VERSION,
+            )
         config.set("app_version", _APP_VERSION)
 
 
@@ -170,8 +179,11 @@
 
     DATA_DIR.mkdir(parents=True, exist_ok=True)
 
+    # ERR-01 FIX: add download_history.jsonl so existing users don't lose
+    # their history on first launch after upgrading to v16.
     _LEGACY_MAP = {
-        APP_BINARY_DIR / "config.json": DATA_DIR / "config.json",
+        APP_BINARY_DIR / "config.json":            DATA_DIR / "config.json",
+        APP_BINARY_DIR / "download_history.jsonl": DATA_DIR / "download_history.jsonl",
     }
 
     for src, dst in _LEGACY_MAP.items():
diff -ruN '--exclude=*.pyc' '--exclude=__pycache__' omnidl_audit3/OmniDL-main/ui/tabs/settings_tab.py omnidl_fixed/ui/tabs/settings_tab.py
--- omnidl_audit3/OmniDL-main/ui/tabs/settings_tab.py	2026-03-09 11:22:29.000000000 +0000
+++ omnidl_fixed/ui/tabs/settings_tab.py	2026-03-10 05:04:51.093203161 +0000
@@ -51,8 +51,16 @@
     import zipfile
     from pathlib import Path
 
-    _appdata = os.getenv("APPDATA", str(Path.home()))
-    override_dir = Path(_appdata) / "OmniDL" / "site-packages"
+    # SEC-03 FIX: use platformdirs instead of the APPDATA environment
+    # variable.  A tampered APPDATA value could redirect the install target
+    # to an attacker-controlled directory, prepending a malicious package
+    # onto sys.path (CWE-426: Untrusted Search Path).
+    try:
+        from platformdirs import user_data_dir as _udd
+        _override_base = Path(_udd("OmniDL", appauthor=False))
+    except ImportError:
+        _override_base = Path.home() / ".omnidl"
+    override_dir = _override_base / "site-packages"
     override_dir.mkdir(parents=True, exist_ok=True)
 
     # ── 1. Resolve latest wheel URL + digest from PyPI ───────────────────
@@ -386,15 +394,35 @@
             self._dir_lbl.configure(text=chosen)
 
     def _browse_cookie_file(self) -> None:
+        import shutil
         import tkinter.filedialog as fd
         chosen = fd.askopenfilename(
             title="Select cookies.txt (Netscape format)",
             filetypes=[("Cookie files", "*.txt"), ("All files", "*.*")])
         if not chosen:
             return
-        if not Path(chosen).is_file():
+        chosen_path = Path(chosen).resolve()
+        if not chosen_path.is_file():
             self._app.toast("File not found.", "error")
             return
+        # COOKIE-01 FIX: copy the selected file into the OmniDL data directory
+        # so that _validate_cookie_path() (which only accepts files inside
+        # config_path.parent after the SEC-01 fix) will accept it.
+        data_dir = self._app.config.config_path.parent.resolve()
+        if data_dir not in chosen_path.parents:
+            dest = data_dir / chosen_path.name
+            stem, suffix = chosen_path.stem, chosen_path.suffix
+            counter = 1
+            while dest.exists():
+                dest = data_dir / f"{stem}_{counter}{suffix}"
+                counter += 1
+            try:
+                shutil.copy2(chosen_path, dest)
+            except OSError as exc:
+                self._app.toast(f"Could not copy cookie file: {exc}", "error")
+                return
+            chosen = str(dest)
+            self._app.toast("Cookie file copied to OmniDL data folder.", "info")
         self._app.config.set("cookie_file", chosen)
         self._cf_lbl.configure(text=self._short_cookie_path(chosen))
         self._app.toast("Cookie file set.", "info")
diff -ruN '--exclude=*.pyc' '--exclude=__pycache__' omnidl_audit3/OmniDL-main/utils/helpers.py omnidl_fixed/utils/helpers.py
--- omnidl_audit3/OmniDL-main/utils/helpers.py	2026-03-09 11:22:29.000000000 +0000
+++ omnidl_fixed/utils/helpers.py	2026-03-10 05:04:51.059199975 +0000
@@ -5,6 +5,7 @@
 from __future__ import annotations
 
 import logging
+import os
 import re
 import subprocess
 import sys
@@ -104,9 +105,13 @@
     """
     try:
         if sys.platform == "win32":
-            # Single argument: '/select,<absolute_path>'
-            # No shell=True needed — explorer.exe reads its own argv directly.
-            subprocess.Popen(["explorer", f"/select,{str(path.resolve())}"])
+            # OPEN-1 FIX: use shell=True so cmd.exe preserves the quoted path.
+            # Explorer tokenises its own command line on whitespace; passing the
+            # /select, prefix and path as one argv element (list form) does NOT
+            # work for paths that contain spaces — Explorer opens the Desktop.
+            # shell=True with a quoted string is the correct approach.
+            resolved = str(path.resolve())
+            subprocess.Popen(f'explorer /select,"{resolved}"', shell=True)
         elif sys.platform == "darwin":
             subprocess.Popen(["open", "-R", str(path)], close_fds=True)
         else:
@@ -121,7 +126,9 @@
     """Open a folder in the OS file manager."""
     try:
         if sys.platform == "win32":
-            subprocess.Popen(["explorer", str(path)])
+            # OPEN-2 FIX: os.startfile is the native Windows API for this;
+            # Popen(["explorer", path]) spawns an unnecessary cmd.exe layer.
+            os.startfile(str(path))
         elif sys.platform == "darwin":
             subprocess.Popen(["open", str(path)], close_fds=True)
         else:
