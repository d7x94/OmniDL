# ── Remote API optional dependencies ────────────────────────────────────
# Install these ONLY when you want to enable the Remote API feature
# (api_enabled=True in config.json / Settings → Remote API tab).
#
# Usage:
#   pip install -r requirements-api.txt
#
# For PyInstaller builds that bundle the API:
#   pip install -r requirements.txt -r requirements-api.txt
#
fastapi>=0.111.0              # ASGI web framework for the remote-control API
uvicorn[standard]>=0.29.0     # ASGI server — runs in a daemon thread alongside Tkinter
